import { Stub } from "@/components/app/Stub";

export default function SettingsPage() {
  return (
    <Stub title="Settings" milestone="Milestone 2 (preferences) + 4 (par)">
      <p>
        Tabs: profile, subscription (Stripe Customer Portal), diet &amp; preferences (filters,
        allergens, blocked meats, ingredients, use-soon threshold), pantry defaults (par + threshold
        per ingredient).
      </p>
    </Stub>
  );
}
